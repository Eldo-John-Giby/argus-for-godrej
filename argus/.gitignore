# Secrets — never commit
.env
backend/.env

# Python
__pycache__/
*.pyc
.pytest_cache/

# Node
frontend/node_modules/
frontend/dist/

# Local data
backend/argus.db
storage/

# Demo launcher artifacts
backend/venv/
backend/demo.pid
backend/demo_backend.log
frontend/demo_frontend.log
frontend/demo.pid

# Heavy regenerable assets — NOT needed for the offline demo (code references
# none of these; the live pipeline re-derives everything from shipped weights).
# 3x identical 338MB open_clip checkpoints with no loader in the codebase:
weights/clip/
ml/weights/
backend/weights/
# Oversized/unreferenced detectors we don't ship (yolov8s-world.pt ships in 3 copies):
yolov8x-worldv2.pt
backend/yolov8m-world.pt
# Raw source footage — source of ml/results + clips/, regenerable there:
videos/
# Scratch output
*.ann.mp4
