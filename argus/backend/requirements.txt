# --- Web/API layer (all the demo needs) ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0   # SQLite async driver — the no-setup default database
asyncpg==0.29.0     # only needed for Postgres deployments (docker-compose)
pydantic==2.9.2
httpx==0.27.2
python-multipart==0.0.12  # required by FastAPI form/video-upload endpoints
PyYAML==6.0.2

# --- Perception/ML layer (lazy-imported; NOT needed for the seeded demo) ---
# Install with: pip install -r requirements-ml.txt
# Or standalone: ultralytics==8.3.0 opencv-python-headless==4.10.0.84

# --- Optional niceties ---
gtts==2.5.3  # optional: server-side TTS for voice alerts (frontend falls back to browser TTS)
